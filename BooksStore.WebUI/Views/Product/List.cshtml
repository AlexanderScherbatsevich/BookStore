@model ProductsListViewModel

@{
    ViewBag.Title = "Products";
}

@foreach (var p in Model.Products)
{
    <partial name="_ProductSummary" model="p"/>
}

@*<div class="container">
    <ul class="pagination justify-content-center">
        @for (int i = 1; i <= Model.PagingInfo.TotalPages; i++)
        {
            <li><a asp-route-page="@i" asp-route-category="@Model.CurrentCategoryId" >@i</a></li>
            <li><a asp-route-page="@i">@i</a></li>
        }
    </ul>
</div>*@

<page-link paging-info="Model.PagingInfo" page-action="List"></page-link>

